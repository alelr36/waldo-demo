import React from 'react';
import PizzaPicker from '../PizzaPicker';
import Cart from '../Cart';

const Home = () => {
    return (
        <div>
            <PizzaPicker />
            <Cart />
        </div>
    );
};

export default Home;